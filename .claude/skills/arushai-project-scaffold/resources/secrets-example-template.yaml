# {PROJECT_NAME} — Secrets Configuration (EXAMPLE)
# Copy this file to config/secrets.yaml and fill in real values.
# config/secrets.yaml is GITIGNORED — never commit actual secrets.

# database:
#   url: "postgresql://user:password@localhost:5432/{project_name}"

# api_keys:
#   openai: "sk-..."
#   anthropic: "sk-ant-..."

# auth:
#   jwt_secret: "your-jwt-secret-here"
